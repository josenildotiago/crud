        <div class="form-group">
            <label for="{{column}}">{{column}}</label>
            <input type="text" name="{{column}}" value="{{column}}" class="form-control" id="{{column}}" placeholder="{{column}}">
        </div>
